services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Persiste los datos JSON fuera del contenedor
      - ./data:/app/server/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATA_DIR=/app/server/data
    restart: unless-stopped
